network:
  name: cvae_encoder
  gamma: 10
  num_classes: '@{dataset.num_classes}'
  pretrained: True          # set 'True' to load pretrained model
  checkpoint: ./results/cifar10_cvae_encoder_base_default/s0/best.ckpt  # ignore if pretrained is false
  num_gpus: '@{num_gpus}'
