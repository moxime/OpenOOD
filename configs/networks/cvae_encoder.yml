network:
  name: cvae_encoder
  gamma: 0.
  job_number: 343928
  num_classes: '@{dataset.num_classes}'
  pretrained: True          # set 'True' to load pretrained model
  checkpoint: ./results/cifar10_cvae_encoder_'@{network.job_number}'_base_default/s0/best.ckpt  # ignore if pretrained is false
  num_gpus: '@{num_gpus}'
