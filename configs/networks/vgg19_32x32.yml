network:
  name: vgg19_32x32
  num_classes: '@{dataset.num_classes}'
  pretrained: False          # set 'True' to load pretrained model
  checkpoint: ./results/cifar10_vgg19_32x32_base_e100_lr0.01_default/s0/best.ckpt  # ignore if pretrained is false
  num_gpus: '@{num_gpus}'
